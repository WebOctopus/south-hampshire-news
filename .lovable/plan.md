

## Change "Payment Setup Required" to "Booking Terms Accepted"

**File: `src/components/dashboard/BookingCard.tsx`**

Update `getPaymentStatusLabel` function (lines 144-153) to return `'Booking Terms Accepted'` instead of `'Payment Setup Required'` in both places (lines 145 and 152).

