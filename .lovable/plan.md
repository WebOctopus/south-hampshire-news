

## Update Artwork Format Requirements in Design Fee Step

### Change
In `src/components/DesignFeeStep.tsx`, update the default text for the "no design" note (line 161) from:

> "Artwork must be supplied in high-resolution print-ready format (PDF, AI, or EPS)"

to:

> "Artwork must be supplied in high-resolution (300dpi) print-ready format (PDF, JPG)"

### File
- `src/components/DesignFeeStep.tsx` — line 161, update the `noNote` default string

