

## Remove Enquiry Form from Advertising Page

Remove the `<EnquiryFormSection />` usage from `src/pages/Advertising.tsx`:
- Remove the import of `EnquiryFormSection` (line 37)
- Remove the comment and component render (lines 1229-1230)

The `EnquiryFormSection` component file itself can be kept in case it's needed elsewhere later.

